<section>
    <h2 class="text-center py-4 bg-danger">
        @if (@isset($viewData["subtitle_custom"]))
            Day la mota section abc: {{ $viewData["subtitle_custom"] }}
        @else
            Day la mota mac dinh section abc
        @endif
    </h2>
</section>